{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: g++-9 生成活动文件",
      "command": "/usr/bin/g++-10",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}",
        "-lmy_http","-lmy_net","-lmy_base",
        "-lpthread"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "调试器生成的任务。"
    }
  ],
  "version": "2.0.0"
}